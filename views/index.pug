extends layout

block content
  h1= title
  p Welcome to #[em LocalLibrary], a very basic Express website developed as a tutorial example on the Mozilla Developer Network.

  h1 Dynamic content

  if error
    p Error getting dynamic content.
  else
    p The library has the following record counts:

    ul.record-list
      li #[strong Books:] 
        a(href='/catalog/books') !{data.book_count}
      li #[strong Copies:] 
        a(href='/catalog/books') !{data.book_instance_count}
      li #[strong Copies available:]
        a(href='/catalog/books') !{data.book_instance_available_count} 
      li #[strong Authors:]
        a(href='/catalog/books') !{data.author_count}
      li #[strong Genres:] 
        a(href='/catalog/genres') !{data.genre_count}